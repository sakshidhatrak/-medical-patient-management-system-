// SQLite local datasource tests removed — app now uses Supabase on web.
// Mobile offline tests to be added when native build is implemented.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () => expect(true, isTrue));
}
